# Copy to .env and set the detector and fusion laptops' addresses (Tailscale
# MagicDNS names or tailnet IPs). Defaults assume you named the machines
# "detector" and "fusion".
DETECTOR_HOST=detector
FUSION_HOST=fusion
