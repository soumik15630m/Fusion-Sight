# Copy to .env and set your laptop's LAN IPv4 (from `ipconfig` -- the active
# Wi-Fi/Ethernet adapter, e.g. 192.168.1.50). Phones connect to
# https://<LAN_HOST>:8443/... and must be on the SAME Wi-Fi.
LAN_HOST=192.168.1.50
